{
  "activeGoalId": null,
  "goals": [
    {
      "id": "UG-1",
      "description": "Unconditionally bind MemoryCortex in src/swarm/engine.ts with in-memory persistence and includeShared cross-app retrieval",
      "status": "completed",
      "verificationCriteria": "Engine initializes MemoryCortex even when QDRANT_URL is omitted; workflow uses in-memory vector fallback to retrieve baselines and distill exemplars; supports params.cortex injection and settings.includeShared",
      "evidence": "Bound MemoryCortex unconditionally in src/swarm/engine.ts using defaultCortexRegistry per appId and shared fallbackStore in MemoryCortex; supported params.cortex injection, settings.includeShared, and fast-path exemplar persistence; verified via Step 18 in test-portable-swarm.ts; tsc and npm test passed with 0 errors."
    },
    {
      "id": "UG-2",
      "description": "Implement cross-provider Critic routing and fast-path learning capture in src/swarm/engine.ts",
      "status": "completed",
      "verificationCriteria": "Dedicated critic/verifier agent or alternative provider is selected as critic; fast-path executions store baseline exemplars into MemoryCortex",
      "evidence": "Implemented dedicated critic resolution (supporting id: 'critic' or role: 'verifier'/'critic') and automatic cross-provider Critic selection to prevent LLM self-affirmation bias; added fast-path learning capture with heuristic feedback string; verified via Step 19 in test-portable-swarm.ts; tsc and npm test passed with 0 errors."
    },
    {
      "id": "UG-3",
      "description": "Add automated memory consolidation triggers and configurable dense/sparse RRF weights in MemoryCortex",
      "status": "completed",
      "verificationCriteria": "Automatic pruning of low-rated memories (<0.40) triggers after threshold store count; RRF accepts denseWeight and sparseWeight factors",
      "evidence": "Added denseWeight and sparseWeight to RetrievalOptions and weighted RRF ranking in retrieveFromFallback; added autoConsolidateThreshold, storesSinceConsolidation tracking, and getPendingConsolidationCount in MemoryCortex; verified via Step 20 in test-portable-swarm.ts."
    },
    {
      "id": "UG-4",
      "description": "End-to-End multi-session learning verification, typecheck, build, and automated test suite pass",
      "status": "completed",
      "verificationCriteria": "Automated tests verify that sequential workflow executions without Qdrant accumulate positive exemplars, retrieve baselines, and improve responses; tsc and npm test pass",
      "evidence": "Verified sequential workflow executions without Qdrant accumulating positive exemplars and feeding them back to subsequent runs via Step 21 in test-portable-swarm.ts; tsc --noEmit 0 errors, npm run build (client & swarm) clean, all 5 test suites in npm test passed cleanly."
    }
  ]
}
